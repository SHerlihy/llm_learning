import { ResourceBuilder } from "@/content/resourceBuilder";

const llmBenchmarksCooked = new ResourceBuilder('How We Broke Top AI Agent Benchmarks: And What Comes Next by UC Berkeley')
.setOverview('How LLM bencmarks can and are being exploited to give incorrect elevated scores.')
.setHref('https://rdi.berkeley.edu/blog/trustworthy-benchmarks-cont/')
.setDateOfInclusion(new Date(2026, 4, 18, 7))
.setTag('best for')
.build()

export default llmBenchmarksCooked
