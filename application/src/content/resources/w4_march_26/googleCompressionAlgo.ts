import { ResourceBuilder } from "@/content/resourceBuilder";

const googleCompressionAlgo = new ResourceBuilder('TurboQuant: Redefining AI efficiency with extreme compression by Google')
.setHref('https://research.google/blog/turboquant-redefining-ai-efficiency-with-extreme-compression/')
.setDateOfInclusion(new Date(2026, 3, 30, 7))
.setOverview('Compression algorithm that reduces the size of vectors.')
.setTag('commerce')
.setTag('cost')
.setTag('coding')
.build()

export default googleCompressionAlgo
