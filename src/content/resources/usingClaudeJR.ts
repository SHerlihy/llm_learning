import { ResourceBuilder } from "../resourceBuilder";

const usingClaudeJR = new ResourceBuilder('Using Claude by Jag Raheel')
.setDateOfInclusion(new Date(2026, 2, 28))
.setOverview('A software engineer\'s account of how they best use LLMs to assist in development.')
.setHref('https://arrangeactassert.com/posts/no-the-sun-does-not-shine-out-of-claudes-backside/')
.setTag('coding')
.build()

export default usingClaudeJR
